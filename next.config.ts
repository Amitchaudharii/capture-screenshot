import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  reactStrictMode: true,
  images: {
    domains: [
      "capture.296f04d6c5d9e5a488011b334ea36b1e.r2.cloudflarestorage.com",
      "capture.cleanstartsolution.com",
    ],
  },
};

export default nextConfig;
